# 10.4 Integral Test for Convergence

!!! abstract "Keywords"

    **Integral Test 积分判别法** • **Positive 正的** 
    
    **Continuous 连续的** • **Decreasing 递减的** • **Improper Integral 反常积分**

## The Bridge Between Sums and Integrals

The Integral Test states that if the terms of a series can be modeled by a well-behaved function, then the infinite series and the improper integral will share the exact same convergence behavior.

!!! info "Theorem: The Integral Test (LIM-7.A.6)"
    Let $f$ be a function that is **positive**, **continuous**, and **decreasing** for all $x \geq 1$. Let $a_n = f(n)$.
    
    *   If the improper integral $\int_{1}^{\infty} f(x) \, dx$ **converges**, then the series $\sum\limits_{n=1}^{\infty} a_n$ **converges**.
    
    *   If the improper integral $\int_{1}^{\infty} f(x) \, dx$ **diverges**, then the series $\sum\limits_{n=1}^{\infty} a_n$ **diverges**.

---

## Visualizing the Test

Why does this work? We can view the terms of a series $a_n$ as areas of rectangles with a width of $1$. 

In the graph below, the curve is $f(x) = \frac{1}{x^2}$. The points represent the terms of the series $\sum \frac{1}{n^2}$. Since the total area under the curve from $1$ to $\infty$ is finite, the sum of the terms (which are bounded by the curve) must also be finite.

<div id="box_integral_test" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_integral_test', {
      boundingbox: [-1, 1.5, 10, -0.5],
      functions: [
          {expr: '1/(x^2)', domain: [1, 10], color: '#1f77b4'}
      ],
      points: [
          [1, 1, true],
          [2, 0.25, true],
          [3, 0.111, true],
          [4, 0.0625, true],
          [5, 0.04, true]
      ]
  });
</script>

---

## The Three Golden Conditions

Before you can use the Integral Test on the AP Exam, you **must** state and verify that the corresponding function $f(x)$ is:

1.  **Positive:** The terms are above the x-axis.

2.  **Continuous:** There are no vertical asymptotes or holes for $x \geq 1$.

3.  **Decreasing:** The derivatives are negative ($f'(x) < 0$) or the terms clearly shrink.

#### Example: Testing $\sum_{n=1}^{\infty} \frac{1}{n^2}$
Let $f(x) = \frac{1}{x^2}$. This function is positive, continuous, and decreasing on $[1, \infty)$. Let's integrate:

$$ \int_{1}^{\infty} \frac{1}{x^2} \, dx = \lim_{t \to \infty} \left[ -\frac{1}{x} \right]_{1}^{t} = \lim_{t \to \infty} \left( -\frac{1}{t} + 1 \right) = 1 $$

Since the integral converges to $1$, the series $\sum_{n=1}^{\infty} \frac{1}{n^2}$ **converges**. (Note: This does *not* mean the sum of the series is $1$. The integral only shares convergence, not the exact sum value).

---

!!! tip "Extension: Euler's Solution to the Basel Problem"
    We just proved that $\sum_{n=1}^{\infty} \frac{1}{n^2}$ converges using the Integral Test. But what does it converge to? 
    This was known as the *Basel Problem*. In 1734, the brilliant mathematician Leonhard Euler shocked the world by proving that the sum is exactly:
    
    $$ \sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6} \approx 1.6449 $$
    
    It is mind-bending that the infinite sum of squares of reciprocals yields a result involving $\pi$, the circle constant!

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz10-4-1"></div>
<script>
  renderMCQ('quiz10-4-1', {
    title: 'Verifying Conditions',
    question: 'For which of the following series can the Integral Test NOT be used directly?',
    options: [
      'A. <span class="arithmatex">\\( \\sum\\limits_{n=1}^{\\infty} \\frac{1}{n} \\)</span>',
      'B. <span class="arithmatex">\\( \\sum\\limits_{n=1}^{\\infty} \\frac{\\sin(n)}{n^2} \\)</span>',
      'C. <span class="arithmatex">\\( \\sum\\limits_{n=1}^{\\infty} e^{-n} \\)</span>',
      'D. <span class="arithmatex">\\( \\sum\\limits_{n=2}^{\\infty} \\frac{1}{n\\ln(n)} \\)</span>'
    ],
    correctIndex: 1, 
    feedback: {
      correct: "Correct! The terms of <span class='arithmatex'>\\( \\sum \\frac{\\sin(n)}{n^2} \\)</span> alternate between positive and negative because of the sine function. The Integral Test requires the function to be strictly positive.",
      wrong: "Check the three conditions: positive, continuous, and decreasing. One of these options has terms that fluctuate in sign."
    }
  });
</script>

<div id="quiz10-4-2"></div>
<script>
  renderMCQ('quiz10-4-2', {
    title: 'Applying the Test',
    question: 'Consider the series <span class="arithmatex">\\( \\sum\\limits_{n=1}^{\\infty} \\frac{1}{n} \\)</span> (The Harmonic Series). What does the Integral Test conclude about it?',
    options: [
      'A. It converges because the integral yields 1.',
      'B. It diverges because the integral evaluates to <span class="arithmatex">\\( \\ln(\\infty) \\)</span> which is infinite.',
      'C. It converges because <span class="arithmatex">\\( \\lim\\limits_{n \\to \\infty} \\frac{1}{n} = 0 \\)</span>.',
      'D. The test is inconclusive.'
    ],
    correctIndex: 1, 
    feedback: {
      correct: "Correct! <span class='arithmatex'>\\( \\int_{1}^{\\infty} \\frac{1}{x} dx = [\\ln(x)]_{1}^{\\infty} = \\infty \\)</span>. Since the integral diverges, the Harmonic Series also diverges.",
      wrong: "Set up the integral of 1/x from 1 to infinity. Recall that the antiderivative of 1/x is ln|x|."
    }
  });
</script>