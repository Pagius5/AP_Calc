# 10.8 Ratio Test for Convergence

!!! abstract "Keywords"

    **Ratio Test 比值判别法** • **Absolute Convergence 绝对收敛** 
    
    **Factorials 阶乘** • **Exponentials 指数函数** • **Inconclusive 不确定/失效**

## The "Ultimate" Test for Series

The **Ratio Test** is one of the most versatile tools in the series toolkit. It is specifically designed to handle series containing **factorials ($n!$)** or **exponentials ($a^n$)**, where other tests like the Integral Test or Comparison Test might be difficult to apply.

!!! info "核心要点：The Ratio Test (LIM-7.A.11)"
    Let $\sum a_n$ be a series with non-zero terms. Define the limit of the ratio of consecutive terms as:
    
    $$ L = \lim_{n \to \infty} \left| \frac{a_{n+1}}{a_n} \right| $$

    1.  **If $L < 1$:** The series **converges absolutely**.

    2.  **If $L > 1$ (or $L = \infty$):** The series **diverges**.

    3.  **If $L = 1$:** The test is **inconclusive**. (The series could converge or diverge; you must use a different test, such as the $p$-series or Integral test).

---

## When to Use the Ratio Test

The Ratio Test works by comparing the growth of the $(n+1)$-th term to the $n$-th term. 

*   **Factorials:** Terms like $n!$ or $(2n)!$ are perfect for the Ratio Test because the ratio simplifies beautifully: $\frac{(n+1)!}{n!} = n+1$.

*   **Exponentials:** Terms like $3^n$ simplify as $\frac{3^{n+1}}{3^n} = 3$.

#### Example: Evaluating $\sum_{n=1}^{\infty} \frac{2^n}{n!}$

1.  **Set up the ratio:**
    
    $$ \lim_{n \to \infty} \left| \frac{2^{n+1}}{(n+1)!} \cdot \frac{n!}{2^n} \right| $$

2.  **Simplify terms:**
    
    $$ \lim_{n \to \infty} \left| \frac{2 \cdot 2^n}{(n+1) \cdot n!} \cdot \frac{n!}{2^n} \right| = \lim_{n \to \infty} \frac{2}{n+1} $$

3.  **Evaluate the limit:**
    The limit is **0**. Since $0 < 1$, the series converges absolutely.

---

## Visualizing the Ratio

For a series to converge via the Ratio Test, the "multiplier" between terms must eventually settle down to a value less than 1. In the graph below, we plot the ratio $R_n = |a_{n+1}/a_n|$ for the series $\sum \frac{2^n}{n!}$.

<div id="box_ratio_viz" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_ratio_viz', {
      boundingbox: [-1, 2.5, 10, -0.2],
      functions: [
          {expr: '1', domain: [-1, 15], color: '#d62728'} // The "Boundary" L=1
      ],
      points: [
          [1, 1, true],         // R1 = a2/a1 = (4/2)/(2/1) = 1
          [2, 0.666, true],     // R2 = a3/a2 = (8/6)/(4/2) = 0.666
          [3, 0.5, true],       // R3 = a4/a3 = 0.5
          [4, 0.4, true],       // R4 = a5/a4 = 0.4
          [5, 0.333, true],     // R5 = 0.333
          [6, 0.285, true]      
      ]
  });
</script>

Because the ratio values (points) fall and stay below the red boundary line ($L=1$), the series is guaranteed to converge.

---

!!! tip "Extension: The AP Exam Series Checklist"
    According to the official **AP Exclusion Statement**, you are only assessed on these six specific tests:
    
    1.  **nth Term Test** (For divergence only)
    2.  **Integral Test**
    3.  **Comparison Test** (Direct)
    4.  **Limit Comparison Test**
    5.  **Alternating Series Test**
    6.  **Ratio Test**
    
    The Ratio Test is arguably the most crucial for the rest of Unit 10, as it is the primary method used to find the **Radius of Convergence** for Power Series.

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz10-8-1"></div>
<script>
  renderMCQ('quiz10-8-1', {
    title: 'Applying the Ratio Test',
    question: 'Evaluate the limit of the ratio <span class="arithmatex">\\( L = \\lim\\limits_{n \\to \\infty} \\left| \\frac{a_{n+1}}{a_n} \\right| \\)</span> for the series <span class="arithmatex">\\( \\sum\\limits_{n=1}^{\\infty} \\frac{3^n}{n^2} \\)</span>.',
    options: [
      'A. <span class="arithmatex">\\( L = 0 \\)</span>',
      'B. <span class="arithmatex">\\( L = 1 \\)</span>',
      'C. <span class="arithmatex">\\( L = 3 \\)</span>',
      'D. <span class="arithmatex">\\( L = \\infty \\)</span>'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! The ratio is <span class='arithmatex'>\\( \\frac{3^{n+1}}{(n+1)^2} \\cdot \\frac{n^2}{3^n} = 3 \\cdot \\frac{n^2}{(n+1)^2} \\)</span>. As <span class='arithmatex'>\\( n \\to \\infty \\)</span>, <span class='arithmatex'>\\( \\frac{n^2}{(n+1)^2} \\to 1 \\)</span>, so the limit is 3. Since <span class='arithmatex'>\\( 3 > 1 \\)</span>, the series diverges.",
      wrong: "Set up the ratio and simplify the powers of 3. What happens to the ratio of the polynomial parts (n^2 and (n+1)^2) as n becomes very large?"
    }
  });
</script>

<div id="quiz10-8-2"></div>
<script>
  renderMCQ('quiz10-8-2', {
    title: 'Recognizing Inconclusivity',
    question: 'For which of the following series is the Ratio Test inconclusive?',
    options: [
      'A. <span class="arithmatex">\\( \\sum \\frac{n!}{10^n} \\)</span>',
      'B. <span class="arithmatex">\\( \\sum \\frac{1}{n^2} \\)</span>',
      'C. <span class="arithmatex">\\( \\sum \\left(\\frac{1}{2}\\right)^n \\)</span>',
      'D. <span class="arithmatex">\\( \\sum \\frac{n}{e^n} \\)</span>'
    ],
    correctIndex: 1, 
    feedback: {
      correct: "Correct! For any p-series <span class='arithmatex'>\\( \\sum 1/n^p \\)</span>, the ratio <span class='arithmatex'>\\( \\frac{n^p}{(n+1)^p} \\)</span> always approaches 1. The Ratio Test fails for all p-series and rational functions; you must use the p-series test or Integral Test instead.",
      wrong: "The Ratio Test is inconclusive when the limit L = 1. This typically happens with functions that don't have factorials or exponentials to dominate the growth."
    }
  });
</script>