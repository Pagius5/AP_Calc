# 10.10 Alternating Series Error Bound

!!! abstract "Keywords"

    **Alternating Series Error Bound 交错级数误差项** • **Remainder (Error) 余项/误差** 
    
    **Partial Sum 部分和** • **Truncation Error 截断误差**

## Estimating the Sum

When we calculate a limit or a sum, we often use a **partial sum** ($S_n$) as an approximation. The difference between the actual infinite sum ($S$) and our approximation is called the **Remainder** ($R_n$), or simply the **Error**.

$$ \text{Error} = |R_n| = |S - S_n| $$

---

## The Error Bound Theorem

For alternating series, the error is always "trapped" by the next term in the sequence.

!!! info "核心要点：Alternating Series Error Bound (LIM-7.B.1)"
    If an alternating series $\sum (-1)^n a_n$ satisfies the conditions of the **Alternating Series Test** (terms are decreasing in magnitude and $\lim_{n \to \infty} a_n = 0$), then:
    
    The error in using the $n$-th partial sum $S_n$ to approximate the total sum $S$ is less than or equal to the magnitude of the **first omitted term** ($a_{n+1}$).
    
    $$ |S - S_n| \le a_{n+1} $$

---

## Visualizing the Error

Think of the partial sums as a sequence of jumps. Because each jump is smaller than the previous one and in the opposite direction, the actual sum $S$ is always "trapped" between $S_n$ and $S_{n+1}$.

<div id="box_ast_error" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_ast_error', {
      boundingbox: [-1, 1.2, 8, -0.2],
      functions: [
          {expr: '0.693', domain: [-1, 10], color: '#d62728'} // Actual Sum S
      ],
      points: [
          [1, 1, true],         // S1 (Error = 0.307, next term a2 = 0.5)
          [2, 0.5, true],       // S2 (Error = 0.193, next term a3 = 0.333)
          [3, 0.833, true],     // S3 (Error = 0.140, next term a4 = 0.25)
          [4, 0.583, true],     // S4
          [5, 0.783, true]
      ]
  });
</script>

In the graph above, notice that the distance from any point (partial sum) to the red line (actual sum) is **always smaller** than the vertical distance to the next point.

---

## Example: Finding the Error

Approximate the sum of $\sum\limits_{n=1}^{\infty} \frac{(-1)^{n+1}}{n^2}$ using the first 3 terms and find the error bound.

1.  **Partial Sum $S_3$:**
    
    $$ S_3 = 1 - \frac{1}{4} + \frac{1}{9} = \frac{36 - 9 + 4}{36} = \frac{31}{36} \approx 0.8611 $$

2.  **First Omitted Term ($a_4$):**
    The next term in the series (ignoring the sign) is $a_4 = \frac{1}{4^2} = \frac{1}{16} = 0.0625$.

3.  **Conclusion:**
    The actual sum $S$ is within $0.0625$ of $0.8611$. 
    Mathematically: $0.7986 \le S \le 0.9236$.

---

!!! tip "Extension: Error vs. Accuracy"
    In computer science and numerical analysis, this theorem is vital. If you need a result accurate to $10^{-6}$ (six decimal places), you simply find the smallest $n$ such that $a_{n+1} < 0.000001$. This tells you exactly how much work (how many iterations) the computer needs to do to guarantee the result is correct.

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz10-10-1"></div>
<script>
  renderMCQ('quiz10-10-1', {
    title: 'Calculating the Bound',
    question: 'Consider the series <span class="arithmatex">\\( \\sum\\limits_{n=1}^{\\infty} \\frac{(-1)^n}{n!} \\)</span>. If we use the partial sum <span class="arithmatex">\\( S_5 \\)</span> to approximate the total sum, what is the maximum possible error?',
    options: [
      'A. <span class="arithmatex">\\( 1/120 \\)</span>',
      'B. <span class="arithmatex">\\( 1/720 \\)</span>',
      'C. <span class="arithmatex">\\( 1/5 \\)</span>',
      'D. <span class="arithmatex">\\( 1/6 \\)</span>'
    ],
    correctIndex: 1, 
    feedback: {
      correct: "Correct! The error bound is the magnitude of the next term, <span class='arithmatex'>\\( a_6 \\)</span>. Since <span class='arithmatex'>\\( 6! = 720 \\)</span>, the error is at most <span class='arithmatex'>\\( 1/720 \\)</span>.",
      wrong: "The error bound is determined by the term immediately following your partial sum. If you stop at n=5, look at the n=6 term."
    }
  });
</script>

<div id="quiz10-10-2"></div>
<script>
  renderMCQ('quiz10-10-2', {
    title: 'Theory of Error Bounds',
    question: 'Which condition must be met to use the formula <span class="arithmatex">\\( |S - S_n| \\le a_{n+1} \\)</span>?',
    options: [
      'A. The series must be geometric.',
      'B. The series must be absolutely convergent.',
      'C. The series must satisfy the conditions of the Alternating Series Test.',
      'D. The limit of the terms must be infinity.'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! This specific error bound only applies to alternating series that are convergent by the AST (decreasing terms and limit to zero).",
      wrong: "Think about the name of the theorem. It is the 'Alternating Series' error bound. What test determines if such a series is well-behaved?"
    }
  });
</script>