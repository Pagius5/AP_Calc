# 10.7 Alternating Series Test for Convergence

!!! abstract "Keywords"

    **Alternating Series 交错级数** • **Alternating Series Test (AST) 交错级数判别法** 
    
    **Decreasing Magnitude 递减幅值** • **Conditional Convergence 条件收敛**

## What is an Alternating Series?

An alternating series is a series whose terms alternate between positive and negative signs. It typically takes one of these two forms:

$$ \sum_{n=1}^{\infty} (-1)^n a_n \quad \text{or} \quad \sum_{n=1}^{\infty} (-1)^{n+1} a_n $$

where $a_n > 0$ for all $n$.

---

## The Alternating Series Test (AST)

The AST is a relatively "easy" test because it only requires two conditions to be met for the series to converge.

!!! info "核心要点：The Alternating Series Test (LIM-7.A.10)"
    An alternating series $\sum (-1)^n a_n$ **converges** if it satisfies the following two conditions:

    1.  **Decreasing Magnitude:** The terms (ignoring the sign) must be non-increasing. That is, $a_{n+1} \le a_n$ for all $n$ beyond some point.

    2.  **Limit is Zero:** The limit of the terms must be zero: $\lim\limits_{n \to \infty} a_n = 0$.

If both conditions are met, the series is guaranteed to converge.

---

## Visualizing Alternating Convergence

In an alternating series, the partial sums $S_n$ "bounce" back and forth. However, if the terms are getting smaller and heading toward zero, the "bounce" becomes smaller and smaller, eventually trapping the sum at a single finite value.

<div id="box_ast_viz" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_ast_viz', {
      boundingbox: [-1, 1.2, 10, -0.2],
      functions: [
          {expr: '0.693', domain: [-1, 15], color: '#d62728'} // The limit S = ln(2)
      ],
      points: [
          [1, 1, true],         // S1 = 1
          [2, 0.5, true],       // S2 = 1 - 0.5 = 0.5
          [3, 0.833, true],     // S3 = 0.5 + 0.33 = 0.833
          [4, 0.583, true],     // S4 = 0.833 - 0.25 = 0.583
          [5, 0.783, true],     // S5 = 0.583 + 0.2 = 0.783
          [6, 0.616, true]      // S6 = 0.783 - 0.167 = 0.616
      ]
  });
</script>

The points above represent the partial sums of the **Alternating Harmonic Series** $\sum \frac{(-1)^{n+1}}{n}$. Notice how they "squeeze" toward the limit (red line).

---

## Important Distinctions

1.  **Failure of Condition 2:** If $\lim\limits_{n \to \infty} a_n \neq 0$, the series **diverges** by the **nth Term Test**. 

2.  **Failure of Condition 1:** If the terms don't decrease, the series might still diverge or oscillate, and the AST cannot be used to prove convergence.

!!! tip "Extension: Absolute vs. Conditional Convergence"
    The Alternating Harmonic Series $\sum \frac{(-1)^{n+1}}{n}$ is a special case. 
    
    *   The series itself **converges** (by AST).
    *   If you take the absolute value of the terms, you get the Harmonic Series $\sum \frac{1}{n}$, which **diverges**.
    
    When a series converges as an alternating series but diverges when all terms are made positive, we call it **Conditionally Convergent**. If it converges even with absolute values, it is **Absolutely Convergent**.

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz10-7-1"></div>
<script>
  renderMCQ('quiz10-7-1', {
    title: 'Testing AST Conditions',
    question: 'Consider the series <span class="arithmatex">\\( \\sum\\limits_{n=1}^{\\infty} (-1)^n \\frac{n+1}{n} \\)</span>. Does this series converge by the AST?',
    options: [
      'A. Yes, because it is alternating.',
      'B. Yes, because the terms are decreasing.',
      'C. No, because the limit of the terms is not zero.',
      'D. No, because the terms are not decreasing.'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! <span class='arithmatex'>\\( \\lim\\limits_{n \\to \\infty} \\frac{n+1}{n} = 1 \\)</span>. Since the limit of the terms is not zero, the series fails the second condition of the AST and actually diverges by the nth Term Test.",
      wrong: "Check the limit of the terms (ignoring the negative signs) as n goes to infinity. What must that limit be for the AST to guarantee convergence?"
    }
  });
</script>

<div id="quiz10-7-2"></div>
<script>
  renderMCQ('quiz10-7-2', {
    title: 'Conditional Convergence',
    question: 'The series <span class="arithmatex">\\( \\sum\\limits_{n=1}^{\\infty} \\frac{(-1)^n}{\\sqrt{n}} \\)</span> is:',
    options: [
      'A. Absolutely Convergent',
      'B. Conditionally Convergent',
      'C. Divergent',
      'D. Oscillating without a limit'
    ],
    correctIndex: 1, 
    feedback: {
      correct: "Correct! The series converges by AST (terms go to 0 and decrease). However, the absolute value version is <span class='arithmatex'>\\( \\sum 1/\\sqrt{n} \\)</span>, which is a p-series with <span class='arithmatex'>\\( p=0.5 \\)</span>, which diverges. Therefore, it is conditionally convergent.",
      wrong: "First, use AST: does it converge? Second, check the p-series with absolute values (1/n^0.5): does that converge?"
    }
  });
</script>