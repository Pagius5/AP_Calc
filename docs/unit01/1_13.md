# 1.13 Removing Discontinuities

!!! abstract "Keywords"

    **Removing Discontinuities 消除间断点** • **Extended Function 扩展函数** 
    
    **Point Redefinition 点重定义** • **Piecewise Boundary 分段边界**

## The Concept of "Repairing" a Function

If a function $f(x)$ has a removable discontinuity at $x=c$, it means the limit exists ($\lim\limits_{x \to c} f(x) = L$), but the function is either undefined or has a different value at that point. We can "remove" this discontinuity by defining a new function that fills the hole.

!!! info "核心要点：Removing a Discontinuity (LIM-2.C.1)"
    A discontinuity at $x=c$ is removable if $\lim\limits_{x \to c} f(x) = L$ exists as a finite number. 
    
    To remove the discontinuity, we define a new function $g(x)$ such that:
    
    $$ g(x) = \begin{cases} f(x), & x \neq c \\ L, & x = c \end{cases} $$
    
    Since the new function value now matches the limit, the function $g(x)$ is continuous at $x=c$.

---

## Piecewise Continuity at the Boundary

For piecewise-defined functions, the "boundary" is the $x$-value where the formula changes. To ensure continuity across this boundary, we must satisfy the limits from both sides.

!!! info "核心要点：Boundary Continuity (LIM-2.C.2)"
    For a piecewise function to be continuous at a boundary $x=c$:
    
    1.  The limit from the left ($\lim\limits_{x \to c^-} f(x)$) must exist.
    
    2.  The limit from the right ($\lim\limits_{x \to c^+} f(x)$) must exist.
    
    3.  Both limits must equal each other **and** equal the function value $f(c)$.

---

## Example: Filling the Hole

Consider the function $f(x) = \frac{x^2 - 16}{x - 4}$. 

Direct substitution gives $0/0$ at $x=4$. Algebraically, we can simplify:
$$ f(x) = \frac{(x-4)(x+4)}{x-4} = x+4, \quad x \neq 4 $$

The limit as $x \to 4$ is $4+4 = 8$. However, the original function is undefined at $x=4$. To remove this discontinuity, we redefine the function as:

$$ g(x) = \begin{cases} \frac{x^2 - 16}{x - 4}, & x \neq 4 \\ 8, & x = 4 \end{cases} $$

<div id="box_remove_hole" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_remove_hole', {
      boundingbox: [-2, 12, 10, -2],
      functions: [{expr: 'x + 4', domain: [-5, 15], color: '#1f77b4'}],
      points: [[4, 8, true]] // Hole is now filled with a solid point
  });
</script>

---

!!! tip "Extension: Can all discontinuities be removed?"
    No. Only **Removable Discontinuities** (holes) can be removed. 
    
    *   **Jump Discontinuities** cannot be removed because the limit does not exist (the two sides don't meet). 
    *   **Infinite Discontinuities** cannot be removed because the function values grow to infinity; you cannot "plug" a vertical asymptote with a single point to make it continuous.

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz1-13-1"></div>
<script>
  renderMCQ('quiz1-13-1', {
    title: 'Removing a Hole',
    question: 'Let <span class="arithmatex">\\( f(x) = \\frac{x^2 - x - 6}{x - 3} \\)</span> for <span class="arithmatex">\\( x \\neq 3 \\)</span>. What value must be assigned to <span class="arithmatex">\\( f(3) \\)</span> to make the function continuous at <span class="arithmatex">\\( x = 3 \\)</span>?',
    options: [
      'A. <span class="arithmatex">\\( 0 \\)</span>',
      'B. <span class="arithmatex">\\( 3 \\)</span>',
      'C. <span class="arithmatex">\\( 5 \\)</span>',
      'D. The discontinuity cannot be removed.'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! Factor the numerator: <span class='arithmatex'>\\( (x-3)(x+2) \\)</span>. Cancel <span class='arithmatex'>\\( (x-3) \\)</span> to find the limit: <span class='arithmatex'>\\( \\lim\\limits_{x \\to 3} (x+2) = 5 \\)</span>. Assigning <span class='arithmatex'>\\( f(3)=5 \\)</span> fills the hole.",
      wrong: "First, find the limit of the function as x approaches 3 by factoring. That limit value is the value that will 'remove' the discontinuity."
    }
  });
</script>

<div id="quiz1-13-2"></div>
<script>
  renderMCQ('quiz1-13-2', {
    title: 'Continuity at the Boundary',
    question: 'Which of the following conditions is required for a piecewise function <span class="arithmatex">\\( f(x) \\)</span> to be continuous at the boundary <span class="arithmatex">\\( x = a \\)</span>?',
    options: [
      'A. <span class="arithmatex">\\( f(a) \\)</span> must be undefined.',
      'B. <span class="arithmatex">\\( \\lim\\limits_{x \\to a^-} f(x) = \\lim\\limits_{x \\to a^+} f(x) = f(a) \\)</span>',
      'C. The function must have a vertical asymptote at <span class="arithmatex">\\( x = a \\)</span>.',
      'D. The slopes of the two pieces must be equal at <span class="arithmatex">\\( x = a \\)</span>.'
    ],
    correctIndex: 1, 
    feedback: {
      correct: "Correct! This is the fundamental definition of continuity: the left limit, right limit, and point value must all be the same.",
      wrong: "Review the three-step test for continuity. Option B is the mathematical summary of that test."
    }
  });
</script>