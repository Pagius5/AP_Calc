# 1.9 Connecting Multiple Representations of Limits

!!! abstract "Keywords"

    **Graphical Representation 图像表示** • **Numerical Representation 数值表示** 
    
    **Algebraic/Analytic Representation 代数/解析表示** • **Consistency 一致性**

## The "Rule of Four"

To truly understand a limit, you must be able to describe it in four different ways. If a limit exists, all representations must provide a consistent conclusion.

!!! info "核心要点：一致性原则 (The Consistency Principle)"
    For a function $f(x)$ at $x=c$:
    
    1.  **Algebraically:** Simplifying the formula $\frac{0}{0}$ reveals a specific value $L$.
    
    2.  **Graphically:** The curve approaches a $y$-value of $L$ from both sides (even if there is a hole).
    
    3.  **Numerically:** A table of values shows $f(x)$ converging to $L$ as $x$ gets closer to $c$.
    
    4.  **Verbally:** You can explain that "as $x$ gets arbitrarily close to $c$, $f(x)$ stays within an epsilon-distance of $L$."

---

## Example: Connecting a Single Function

Consider the function: $f(x) = \frac{x-3}{x^2-9}$

### 1. Algebraic Representation
We find the limit as $x \to 3$ by factoring:

$$ \lim_{x \to 3} \frac{x-3}{(x-3)(x+3)} = \lim_{x \to 3} \frac{1}{x+3} = \frac{1}{6} \approx 0.1667 $$

### 2. Numerical Representation
If we pick values near $x = 3$:

| $x$    | 2.9    | 2.99   | 3.01   | 3.1    |
| :----- | :----- | :----- | :----- | :----- |
| $f(x)$ | 0.1695 | 0.1669 | 0.1664 | 0.1639 |

The values are clearly converging toward $0.166...$

### 3. Graphical Representation
The graph shows a smooth line $y = \frac{1}{x+3}$ with a **removable discontinuity** (hole) at $x = 3$.

<div id="box_multi_rep" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_multi_rep', {
      boundingbox: [0, 0.5, 6, -0.1],
      functions: [{expr: '1/(x+3)', domain: [-2, 10]}],
      points: [[3, 0.1667, false]] 
  });
</script>

---

## Strategic Thinking: When to use which?

*   **Use Algebra** when you have a precise formula and need an exact answer.
*   **Use Graphs** to quickly identify "Limit Does Not Exist" (DNE) scenarios like jumps or asymptotes.
*   **Use Tables** when the function is "black-boxed" (you only have data points) or when the algebra is too difficult to solve manually.

---

!!! tip "Extension: Data-Driven Calculus"
    In real-world engineering and physics, we often don't have an algebraic formula $f(x)$. Instead, we have high-frequency sensor data (a **Numerical** representation). Determining limits from this data allows scientists to predict "breaking points" or "steady states" before they actually occur.

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz1-9-1"></div>
<script>
  renderMCQ('quiz1-9-1', {
    title: 'Connecting Table to Graph',
    question: 'A table shows that as <span class="arithmatex">\\( x \\to 2^- \\)</span>, <span class="arithmatex">\\( f(x) \\to 5 \\)</span>, and as <span class="arithmatex">\\( x \\to 2^+ \\)</span>, <span class="arithmatex">\\( f(x) \\to -5 \\)</span>. Which graphical feature would you expect to see at <span class="arithmatex">\\( x = 2 \\)</span>?',
    options: [
      'A. A hole (removable discontinuity)',
      'B. A vertical asymptote',
      'C. A jump discontinuity',
      'D. A continuous solid line'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! Because the left-hand limit (5) and right-hand limit (-5) are different finite numbers, the graph 'jumps' from one value to the other.",
      wrong: "If the limits approach two different finite numbers, the graph cannot be connected or just have a single hole."
    }
  });
</script>

<div id="quiz1-9-2"></div>
<script>
  renderMCQ('quiz1-9-2', {
    title: 'Analytic to Graphic Translation',
    question: 'If <span class="arithmatex">\\( \\lim\\limits_{x \\to c} f(x) = \\frac{0}{0} \\)</span> is resolved by factoring and canceling <span class="arithmatex">\\( (x-c) \\)</span>, what does this tell you about the graph of <span class="arithmatex">\\( f \\)</span> at <span class="arithmatex">\\( x = c \\)</span>?',
    options: [
      'A. There is a vertical asymptote at <span class="arithmatex">\\( x = c \\)</span>.',
      'B. There is a removable discontinuity (hole) at <span class="arithmatex">\\( x = c \\)</span>.',
      'C. The function is continuous at <span class="arithmatex">\\( x = c \\)</span>.',
      'D. The function value <span class="arithmatex">\\( f(c) \\)</span> must exist.'
    ],
    correctIndex: 1, 
    feedback: {
      correct: "Correct! The indeterminate form 0/0 that can be cancelled algebraically represents a hole in the graph.",
      wrong: "Think about what 'cancelling' a factor means. It means the function is undefined at that point, but the path from both sides leads to the same spot."
    }
  });
</script>