# 1.12 Confirming Continuity over an Interval

!!! abstract "Keywords"

    **Interval Continuity 区间连续性** • **Domain 定义域** 
    
    **Endpoint Continuity 端点连续性** • **Catalog of Functions 函数类别**

## Continuity on an Interval

A function is continuous on an interval if its graph can be drawn without lifting the pencil anywhere within that range. 

!!! info "核心要点：Continuity on an Interval"
    1.  **Open Interval $(a, b)$:** A function $f$ is continuous on $(a, b)$ if it is continuous at every point in the interval.
    
    2.  **Closed Interval $[a, b]$:** A function $f$ is continuous on $[a, b]$ if:
        *   It is continuous on the open interval $(a, b)$.
        *   It is continuous from the right at $a$: $\lim\limits_{x \to a^+} f(x) = f(a)$.
        *   It is continuous from the left at $b$: $\lim\limits_{x \to b^-} f(x) = f(b)$.

---

## The Catalog of Continuous Functions

One of the most important tools in Calculus is knowing which functions are "naturally" continuous. According to **LIM-2.B.2**, the following functions are continuous at every point in their **domains**:

*   **Polynomials:** (e.g., $x^3 - 2x + 1$) — Continuous on $(-\infty, \infty)$.
*   **Rational Functions:** (e.g., $\frac{P(x)}{Q(x)}$) — Continuous everywhere except where the denominator $Q(x) = 0$.
*   **Power Functions:** (e.g., $x^{2/3}$, $\sqrt{x}$) — Continuous on their defined domains.
*   **Exponential & Logarithmic:** (e.g., $e^x$, $\ln x$) — $e^x$ is continuous on $(-\infty, \infty)$; $\ln x$ is continuous on $(0, \infty)$.
*   **Trigonometric Functions:** (e.g., $\sin x$, $\cos x$, $\tan x$) — $\sin$ and $\cos$ are continuous everywhere; $\tan x$ is continuous except at its vertical asymptotes.

---

## Example: Continuity of the Square Root

Consider $f(x) = \sqrt{x}$. Its domain is $[0, \infty)$. 

<div id="box_sqrt" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_sqrt', {
      boundingbox: [-1, 4, 10, -1],
      functions: [{expr: 'sqrt(x)', domain: [0, 20], color: '#1f77b4'}],
      points: [[0, 0, true]] 
  });
</script>

Is it continuous at the endpoint $x=0$? 
Yes, because $\lim\limits_{x \to 0^+} \sqrt{x} = 0$, which matches $f(0)$. Therefore, we say $f(x) = \sqrt{x}$ is continuous on its entire domain $[0, \infty)$.

---

!!! tip "Extension: Continuity vs. Differentiability"
    While every function we can differentiate is continuous, not every continuous function can be differentiated. A classic example is $f(x) = |x|$. It is perfectly continuous on $(-\infty, \infty)$ (you never lift your pencil), but it has a "sharp corner" at $x=0$ where the slope is undefined. We will explore this further in Unit 2!

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz1-12-1"></div>
<script>
  renderMCQ('quiz1-12-1', {
    title: 'Domain and Continuity',
    question: 'On which of the following intervals is the function <span class="arithmatex">\\( f(x) = \\frac{1}{x-4} \\)</span> continuous?',
    options: [
      'A. <span class="arithmatex">\\( [0, 5] \\)</span>',
      'B. <span class="arithmatex">\\( (4, \\infty) \\)</span>',
      'C. <span class="arithmatex">\\( (-\\infty, \\infty) \\)</span>',
      'D. <span class="arithmatex">\\( [4, 10] \\)</span>'
    ],
    correctIndex: 1, 
    feedback: {
      correct: "Correct! The function has an infinite discontinuity at x = 4. Therefore, it is continuous on any interval that does not include 4, such as (4, ∞).",
      wrong: "Check the denominator. Where is the function undefined? Any interval containing that point cannot be an interval of continuity."
    }
  });
</script>

<div id="quiz1-12-2"></div>
<script>
  renderMCQ('quiz1-12-2', {
    title: 'Catalog of Functions',
    question: 'Which statement is true regarding the function <span class="arithmatex">\\( y = \\ln(x) \\)</span>?',
    options: [
      'A. It is continuous on <span class="arithmatex">\\( [0, \\infty) \\)</span>.',
      'B. It is continuous on <span class="arithmatex">\\( (-\\infty, \\infty) \\)</span>.',
      'C. It is continuous on its domain, <span class="arithmatex">\\( (0, \\infty) \\)</span>.',
      'D. It is only continuous at integer values of x.'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! Logarithmic functions are continuous at all points in their domains. Since ln(x) is only defined for x > 0, it is continuous on (0, ∞).",
      wrong: "Recall the domain of a logarithm. Can you take the log of zero or a negative number?"
    }
  });
</script>