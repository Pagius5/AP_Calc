# 1.14 Connecting Infinite Limits and Vertical Asymptotes

!!! abstract "Keywords"

    **Infinite Limit 无穷极限** • **Vertical Asymptote 垂直渐近线** 
    
    **Unbounded Behavior 无界行为** • **Asymptotic Behavior 渐近行为**

## Infinite Limits

An **infinite limit** occurs when the values of a function increase or decrease without bound as $x$ approaches a specific value $c$. 

!!! info "核心要点：Infinite Limits (LIM-2.D.1)"
    We write $\lim\limits_{x \to c} f(x) = \infty$ or $\lim\limits_{x \to c} f(x) = -\infty$ to describe the behavior of the function. 
    
    **Note:** Technically, if a limit equals $\infty$, the limit still **Does Not Exist (DNE)** because infinity is not a real number. However, using the $\infty$ symbol provides more specific information about the function's behavior than just saying "DNE."

---

## Defining Vertical Asymptotes

A vertical asymptote is a vertical line that the graph of a function approaches but never touches as $x$ approaches a certain value.

!!! info "核心要点：Vertical Asymptote Definition (LIM-2.D.2)"
    The line $x = c$ is a **vertical asymptote** of the graph of $f$ if the limit from either the left or the right is infinite. That is:
    
    $$ \lim_{x \to c^+} f(x) = \pm\infty \quad \text{or} \quad \lim_{x \to c^-} f(x) = \pm\infty $$

### Example: $f(x) = \frac{1}{x-2}$

As $x$ approaches $2$ from the right, the values of $f(x)$ grow larger and larger. As $x$ approaches $2$ from the left, they become increasingly negative.

$$ \lim_{x \to 2^+} \frac{1}{x-2} = \infty, \quad \lim_{x \to 2^-} \frac{1}{x-2} = -\infty $$

<div id="box_va" style="width:100%; height:400px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_va', {
      boundingbox: [-2, 10, 6, -10],
      functions: [
          {expr: '1/(x-2)', domain: [-5, 1.95], color: '#1f77b4'},
          {expr: '1/(x-2)', domain: [2.05, 10], color: '#1f77b4'}
      ],
      colors: ['#1f77b4', '#1f77b4']
  });
</script>

Because at least one of the one-sided limits is infinite, the line **$x=2$** is a vertical asymptote.

---

## Finding Asymptotes Analytically

For rational functions, vertical asymptotes typically occur at $x$-values that make the **denominator zero** but the **numerator non-zero**.

1.  **$\frac{0}{0}$ (Indeterminate):** Usually indicates a **hole** (removable discontinuity).

2.  **$\frac{k}{0}$ (where $k \neq 0$):** Indicates an **infinite limit** and therefore a **vertical asymptote**.

---

!!! tip "Extension: One-Sided vs. Two-Sided Infinite Limits"
    Consider $f(x) = \frac{1}{(x-2)^2}$. In this case, both the left-hand and right-hand limits approach $+\infty$. 
    
    $$ \lim_{x \to 2^-} \frac{1}{(x-2)^2} = \infty \quad \text{and} \quad \lim_{x \to 2^+} \frac{1}{(x-2)^2} = \infty $$
    
    Therefore, we can say the general limit $\lim\limits_{x \to 2} \frac{1}{(x-2)^2} = \infty$. Even though it's still technically DNE, the behavior is consistent from both sides.

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz1-14-1"></div>
<script>
  renderMCQ('quiz1-14-1', {
    title: 'Interpreting Infinite Limits',
    question: 'If <span class="arithmatex">\\( \\lim\\limits_{x \\to 3^-} f(x) = \\infty \\)</span>, which of the following must be true?',
    options: [
      'A. The limit <span class="arithmatex">\\( \\lim\\limits_{x \\to 3} f(x) \\)</span> exists.',
      'B. The line <span class="arithmatex">\\( y = 3 \\)</span> is a horizontal asymptote.',
      'C. The line <span class="arithmatex">\\( x = 3 \\)</span> is a vertical asymptote.',
      'D. <span class="arithmatex">\\( f(3) = \\infty \\)</span>'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! By definition, if the limit as x approaches a constant from either side is infinite, then that constant represents the location of a vertical asymptote.",
      wrong: "Check the definition of a vertical asymptote. Does an infinite limit describe a horizontal or vertical line?"
    }
  });
</script>

<div id="quiz1-14-2"></div>
<script>
  renderMCQ('quiz1-14-2', {
    title: 'Asymptote vs. Hole',
    question: 'Consider the function <span class="arithmatex">\\( f(x) = \\frac{x-1}{x^2-1} \\)</span>. Where does the vertical asymptote occur?',
    options: [
      'A. At <span class="arithmatex">\\( x = 1 \\)</span> and <span class="arithmatex">\\( x = -1 \\)</span>',
      'B. Only at <span class="arithmatex">\\( x = 1 \\)</span>',
      'C. Only at <span class="arithmatex">\\( x = -1 \\)</span>',
      'D. There are no vertical asymptotes.'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! Factor the denominator: <span class='arithmatex'>\\( (x-1)(x+1) \\)</span>. The <span class='arithmatex'>\\( (x-1) \\)</span> factor cancels (creating a hole at x=1), but the <span class='arithmatex'>\\( (x+1) \\)</span> remains in the denominator. Since <span class='arithmatex'>\\( f(-1) \\)</span> results in <span class='arithmatex'>\\( k/0 \\)</span>, there is a vertical asymptote at x = -1.",
      wrong: "Be careful! If a factor cancels out, it creates a hole, not an asymptote. Only the non-cancelling factors in the denominator create vertical asymptotes."
    }
  });
</script>