# 1.11 Defining Continuity at a Point

!!! abstract "Keywords"

    **Continuity 连续性** • **Existence 存在性** 
    
    **Point Discontinuity 点间断** • **Three-Step Test 连续性三步检验**

## The Mathematical Definition

A function $f(x)$ is continuous at a point $x=c$ if the graph is "unbroken" there. However, to prove this analytically, we must satisfy three specific conditions.

!!! info "核心要点：The Three Conditions of Continuity"
    A function $f(x)$ is **continuous at $x=c$** if and only if:

    1.  **$f(c)$ exists.** (The function is defined at $c$; there is no hole or asymptote at the point).

    2.  **$\lim\limits_{x \to c} f(x)$ exists.** (The left-hand limit equals the right-hand limit).

    3.  **$\lim\limits_{x \to c} f(x) = f(c)$.** (The limit matches the actual function value).

If **any** of these three conditions are not met, the function is discontinuous at $x=c$.

---

## Visualizing the Conditions

Let's look at a function that **is** continuous at $x = 2$.

<div id="box_continuous" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_continuous', {
      boundingbox: [-1, 5, 5, -1],
      functions: [{expr: 'x', domain: [-5, 10], color: '#1f77b4'}],
      points: [[2, 2, true]] 
  });
</script>

*   **Condition 1:** $f(2) = 2$ (The solid point exists).
*   **Condition 2:** $\lim\limits_{x \to 2} f(x) = 2$ (The path from both sides leads to $y=2$).
*   **Condition 3:** The limit ($2$) equals the point ($2$).
*   **Result:** Continuous at $x=2$.

---

## Piecewise Continuity: Solving for a Constant

A classic AP-style problem asks you to find a value for a constant (like $k$) that makes a piecewise function continuous.

**Example:** Find the value of $k$ that makes $f(x)$ continuous at $x=3$:
$$ f(x) = \begin{cases} x^2 - 1, & x \le 3 \\ kx + 2, & x > 3 \end{cases} $$

To be continuous, the limit from the left must equal the limit from the right:

1.  **Left-hand limit:** $\lim\limits_{x \to 3^-} (x^2 - 1) = 3^2 - 1 = 8$.

2.  **Right-hand limit:** $\lim\limits_{x \to 3^+} (kx + 2) = 3k + 2$.

3.  **Set them equal:** $8 = 3k + 2 \implies 6 = 3k \implies k = 2$.

---

!!! tip "Extension: Continuity on an Interval"
    We say a function is continuous on an **open interval** $(a, b)$ if it is continuous at every point in that interval. For a **closed interval** $[a, b]$, we also check the endpoints using one-sided limits: 
    $\lim\limits_{x \to a^+} f(x) = f(a)$ and $\lim\limits_{x \to b^-} f(x) = f(b)$.

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz1-11-1"></div>
<script>
  renderMCQ('quiz1-11-1', {
    title: 'Testing Continuity Conditions',
    question: 'A function <span class="arithmatex">\\( f(x) \\)</span> has a hole at <span class="arithmatex">\\( x = 4 \\)</span> at a height of <span class="arithmatex">\\( y = 7 \\)</span>, but <span class="arithmatex">\\( f(4) = 2 \\)</span>. Which condition of continuity fails?',
    options: [
      'A. Condition 1: <span class="arithmatex">\\( f(4) \\)</span> does not exist.',
      'B. Condition 2: <span class="arithmatex">\\( \\lim\\limits_{x \\to 4} f(x) \\)</span> does not exist.',
      'C. Condition 3: <span class="arithmatex">\\( \\lim\\limits_{x \\to 4} f(x) \\neq f(4) \\)</span>.',
      'D. All conditions are satisfied.'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! <span class='arithmatex'>\\( f(4) \\)</span> exists (it is 2) and the limit exists (it is 7), but they are not equal to each other.",
      wrong: "Check the values given. Does the point exist? Does the limit exist? Do they match?"
    }
  });
</script>

<div id="quiz1-11-2"></div>
<script>
  renderMCQ('quiz1-11-2', {
    title: 'Solving for Continuity',
    question: 'For what value of <span class="arithmatex">\\( k \\)</span> is the function <span class="arithmatex">\\( g(x) = \\begin{cases} kx + 5, & x < 2 \\\\ x^2 - k, & x \\ge 2 \\end{cases} \\)</span> continuous at <span class="arithmatex">\\( x = 2 \\)</span>?',
    options: [
      'A. <span class="arithmatex">\\( k = -1/3 \\)</span>',
      'B. <span class="arithmatex">\\( k = 1 \\)</span>',
      'C. <span class="arithmatex">\\( k = -3 \\)</span>',
      'D. <span class="arithmatex">\\( k = 0 \\)</span>'
    ],
    correctIndex: 0, 
    feedback: {
      correct: "Correct! Set the two pieces equal at <span class='arithmatex'>\\( x=2 \\)</span>: <br> <span class='arithmatex'>\\( 2k + 5 = 2^2 - k \\)</span> <br> <span class='arithmatex'>\\( 2k + 5 = 4 - k \\)</span> <br> <span class='arithmatex'>\\( 3k = -1 \\implies k = -1/3 \\)</span>.",
      wrong: "Set the left-hand limit equal to the right-hand limit at x = 2 and solve for k."
    }
  });
</script>