# 1.6 Determining Limits Using Algebraic Manipulation

!!! abstract "Keywords"

    **Indeterminate Form 不定式** • **Factoring 因式分解** 
    
    **Conjugates 共轭** • **Trigonometric Identities 三角恒等式** • **Rationalizing 有理化**

## The Indeterminate Form $\frac{0}{0}$

When evaluating $\lim\limits_{x \to c} \frac{f(x)}{g(x)}$, if direct substitution results in $\frac{0}{0}$, the limit is in an **indeterminate form**. This does *not* mean the limit does not exist; rather, it means the limit is "hidden" and we must use algebra to reveal it.

!!! info "核心要点：处理不定式"
    If direct substitution yields $\frac{0}{0}$, you must rearrange the expression into an equivalent form. The goal is usually to "cancel out" the term causing the zero in the denominator.

---

## Strategy 1: Factoring and Dividing

This is the most common technique for rational functions. If $f(c)=0$ and $g(c)=0$, then $(x-c)$ is a factor of both the numerator and denominator.

1.  **Factor** the numerator and denominator completely.

2.  **Cancel** the common factor $(x-c)$.

3.  **Substitute** $x=c$ into the remaining expression.

#### Example: A Hole in the Graph
Evaluate $\lim\limits_{x \to 1} \frac{x^2 - 1}{x - 1}$.
$$ \frac{x^2 - 1}{x - 1} = \frac{(x-1)(x+1)}{x-1} = x+1 $$
Now, substitute $x=1$: $1 + 1 = 2$.

<div id="box_factoring" style="width:100%; height:300px; border-radius: 8px; border: 1px solid #ddd;"></div>
<script src="/javascripts/math_graph.js"></script>
<script>
  renderMathGraph('box_factoring', {
      boundingbox: [-2, 5, 4, -1],
      functions: [{expr: '(x^2 - 1)/(x - 1)', domain: [-5, 5]}],
      points: [[1, 2, false]] 
  });
</script>

---

## Strategy 2: Rationalization (Conjugates)

If the expression contains a square root, multiplying by the **conjugate** can help clear the indeterminate form. Recall that the conjugate of $(a - \sqrt{b})$ is $(a + \sqrt{b})$.

1.  **Identify** the radical expression.

2.  **Multiply** both the numerator and denominator by the conjugate of that expression.

3.  **Simplify** using the difference of squares: $(a-b)(a+b) = a^2 - b^2$.

#### Example:
Evaluate $\lim\limits_{x \to 0} \frac{\sqrt{x+4}-2}{x}$.
Multiplying by $\frac{\sqrt{x+4}+2}{\sqrt{x+4}+2}$:

$$ \frac{(\sqrt{x+4}-2)(\sqrt{x+4}+2)}{x(\sqrt{x+4}+2)} = \frac{(x+4)-4}{x(\sqrt{x+4}+2)} = \frac{x}{x(\sqrt{x+4}+2)} = \frac{1}{\sqrt{x+4}+2} $$

Substituting $x=0$: $\frac{1}{\sqrt{4}+2} = \frac{1}{4}$.

---

## Strategy 3: Trigonometric Simplification

Sometimes we use trigonometric identities to rewrite the expression before substituting.

1.  **Common Identities:** $\tan x = \frac{\sin x}{\cos x}$, $\sin^2 x + \cos^2 x = 1$.

2.  **Special Limits:** (To be mastered in later sections, but helpful now) $\lim\limits_{x \to 0} \frac{\sin x}{x} = 1$.

---

!!! tip "Extension: Removable Discontinuities"
    When we "cancel out" a factor like $(x-c)$ from the denominator, we are identifying a **removable discontinuity** (a hole). Algebraically, we are finding a new function that is identical to the original *everywhere except at $x=c$*. Since the limit only cares about the approach and not the point itself, evaluating the new function gives us the correct limit for the original.

---

## Quiz

<script src="/javascripts/mcq.js"></script>

<div id="quiz1-6-1"></div>
<script>
  renderMCQ('quiz1-6-1', {
    title: 'Factoring Technique',
    question: 'Evaluate <span class="arithmatex">\\( \\lim\\limits_{x \\to 5} \\frac{x^2 - 25}{x - 5} \\)</span>.',
    options: [
      'A. <span class="arithmatex">\\( 0 \\)</span>',
      'B. <span class="arithmatex">\\( 5 \\)</span>',
      'C. <span class="arithmatex">\\( 10 \\)</span>',
      'D. Does Not Exist (DNE)'
    ],
    correctIndex: 2, 
    feedback: {
      correct: "Correct! Factor the numerator: <span class='arithmatex'>\\( (x-5)(x+5) \\)</span>. Cancel the <span class='arithmatex'>\\( (x-5) \\)</span> terms to get <span class='arithmatex'>\\( x+5 \\)</span>. Substituting 5 gives 10.",
      wrong: "Direct substitution gives 0/0. Try factoring the numerator using the difference of squares."
    }
  });
</script>

<div id="quiz1-6-2"></div>
<script>
  renderMCQ('quiz1-6-2', {
    title: 'Conjugate Technique',
    question: 'What is the conjugate of the expression <span class="arithmatex">\\( 3 - \\sqrt{x+1} \\)</span> used to evaluate a limit?',
    options: [
      'A. <span class="arithmatex">\\( 3 + \\sqrt{x+1} \\)</span>',
      'B. <span class="arithmatex">\\( -3 + \\sqrt{x+1} \\)</span>',
      'C. <span class="arithmatex">\\( \\sqrt{x+1} - 3 \\)</span>',
      'D. <span class="arithmatex">\\( 3 - \\sqrt{x-1} \\)</span>'
    ],
    correctIndex: 0, 
    feedback: {
      correct: "Correct! To find the conjugate, you keep the terms the same but change the sign between them.",
      wrong: "The conjugate of <span class='arithmatex'>\\( a - b \\)</span> is <span class='arithmatex'>\\( a + b \\)</span>. Look for the version that changes the minus sign to a plus."
    }
  });
</script>